# SecureOps Assistant — RAG / retrieval pipeline dependencies
# Vector Cartel · AAI Tech Talks Hackathon 2026 · Python 3.11+ recommended
#
# GPU note: the dev machine runs torch on CUDA 12.1 (RTX 4060). The pin below is
# the plain version; for the GPU build install from the PyTorch index:
#     pip install torch==2.5.1 --index-url https://download.pytorch.org/whl/cu121
# For CPU-only, `pip install torch==2.5.1` is fine (retrieval just runs slower).

# --- core retrieval stack ---
chromadb==1.5.9
sentence-transformers==5.6.0
rank-bm25==0.2.2
transformers==5.5.4
torch==2.5.1
numpy>=2.1,<3

# --- ingestion / chunking ---
pypdf==6.13.3
PyYAML==6.0.3

# --- optional: token-count fallback used by chunking.py if the bge tokenizer
#     can't be loaded (transformers -> tiktoken -> heuristic) ---
tiktoken==0.13.0
